import { RegisterForm } from '@/features/auth';

export default function RegisterPage() {
  return <RegisterForm />;
}

export const metadata = {
  title: '新規登録 - フリマアプリ',
  description: '新規登録ページ',
};
